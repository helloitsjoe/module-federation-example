{
  "private": true,
  "workspaces": {
    "packages": [
      "app1",
      "app2"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "start": "APP1_PORT=8081 APP2_PORT=8082 lerna run --parallel start",
    "serve": "APP1_PORT=8081 APP2_PORT=8082 lerna run --parallel serve",
    "clean": "lerna run --parallel clean",
    "test": "lerna run --parallel test"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "name": "module-federation-example"
}
